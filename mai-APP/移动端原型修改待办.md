# 移动端（买家版 App）原型修改待办

> **用途**：每次后台（yunying-PC）PRD / 原型梳理完成、后端原型开发完后，若涉及移动端**买家端（mai-APP）**需要同步修改，把需求记录到本文件。之后在另一个对话窗口中按本文件逐条修改买家端原型。
>
> **目录**：`d:\shimh_work\zhuji-wayecheng\wazhanggui-code\mai-APP`
>
> **对应的卖家端（shanghu-APP）改动**单独记录在 `shanghu-APP/移动端原型修改待办.md`，本文件**只**记录买家端需求，互不混放。
>
> **修改标准（高保真原型）**：模拟数据相对真实、按钮/卡片均可点击、状态丰富、内容齐全即可。**不要**搭建"前端读取/同步后台数据"这类架构（如共享 config 文件模拟前后端同步）。
>
> **状态图例**：`待办` → `进行中` → `已完成`
>
> **Agent 执行提示**：每条含"文件 + 精确位置（DOM id / JS 函数 / 行号）"，直接照位置改即可。

---

## 当前待办清单

### 1. 恢复「我的研学」列表中的"待签署"状态 + "去签署"入口 〔待办〕
- **功能模块**：研学模块 → 买家端「研学报名详情 / 我的研学」（`study-my.html`）
- **关联后台 PRD**：研学管理 14a —— 报名状态机（待审核 / **待签署** / 已签署 / 待出行 / 已完成 / 已取消）、责任书模板（个人 7 条 / 团体 5 条）
- **精确位置**：
  - 数据：`var MY_REG`（约第 406 行）—— 当前两项均无 `status` 字段
  - 列表渲染：`function renderMyRegList()`（约第 417–437 行），列表容器 `#myRegList`（约第 73 行）
  - "去签署"按钮逻辑：第 428–429 行 `if(st==='unsigned'){ ... <a href="study-my.html?id=...">去签署</a> ... }`
  - 详情区状态：`function renderPage()` 的 `unsigned` 分支（约第 197–216 行，含"待签署"徽标 + "签署责任书"进度步）——**已存在，保留不动**
- **现状**：详情区本就支持"待签署"（状态机、徽标、签署面板齐全）；但列表默认 `var st='signed'`（第 420 行），且 `MY_REG` 数据项无 `status`，导致列表永远显示"已签署"，看不到"待签署"项与"去签署"入口。
- **改动**：
  1. `MY_REG` 第 1 项补 `status:'unsigned'`（待签署），第 2 项补 `status:'signed'`（已签署）。
  2. `renderMyRegList` 第 420 行改为 `var st=m.status||'signed';`（localStorage 有值则覆盖，无值用数据自带状态）。
  3. 确认 `st==='unsigned'` 时渲染"去签署"按钮（已存在，无需重写）；点击进入 `study-my.html?id=1` 复用详情区已有"签署责任书"流程（个人版 7 条）。

### 2. 「团体研学申请」增加"待补材料"状态 + "补充材料"按钮 〔待办〕
- **功能模块**：研学模块 → 买家端「团体研学申请」（`study-apply.html`）
- **关联后台 PRD**：研学管理 14a —— 报名审核「要求补材料」状态（审核退回 + 退回原因 + 重新提交）
- **精确位置**：
  - 状态回显容器：`#applyStatus`（约第 147 行，`display:none`，提交后由 JS 显示）
  - 渲染函数：`function submitForm(e)` → `markSubmitted()` + 拼接 `#applyStatus` 内容（约第 209–239 行）
  - 步骤徽标：`setStepBadge()`，回显区当前只渲染"申请已提交，等待审核"+"查看我的报名"+"返回研学"（第 226–236 行）
- **现状**：`#applyStatus` 只有"已提交待审核"一种结果态，没有"审核退回 / 待补材料"分支，也没有"补充材料"入口；对应后台"要求补材料"在买家端无体现。
- **改动**：
  1. 新增状态分支（如 `apply.status==='needMaterial'`），在 `#applyStatus` 渲染：状态徽标"待补材料" + 退回原因文案 + 「补充材料」按钮（点击 `toast('请补充材料后重新提交')` 或重新展开表单）。
  2. 同步把"审核状态"流程步（Step 2）徽标置为"已退回/待补材料"。
  3. （可选）在 `study-my.html` 详情区的"审核状态行"（约第 181/188/220–224 行 `#auditStatusRow`）镜像同一"待补材料"状态，保持两页一致。

### 3. 「研学首页」增加"师资团队"和"课程体系"两个区块 〔已取消 · 2026-09-10〕
- **取消原因**：2026-09-10 后台「研学宣传配置」模块已移除「师资力量」「课程体系」两个配置项（与专题 Banner、宣传视频一并移除，仅保留自动生成的「往期风采」）。既然后台不再维护这两类数据、且在移动端也无对应展示位，买家端无需新增该区块。
- **功能模块**：研学模块 → 买家端「研学首页 / 研学宣传」（`study-tour.html`）
- **关联后台 PRD**：研学线路活动管理 / 研学基地管理 —— 基地与路线的"师资""课程体系"属性（高保真需展示）
- **精确位置**：
  - 现有区块顺序（参考注释）：「精选研学路线」`#routeList`（约第 125–172 行）→「研学基地介绍」（约第 174 行起）→ 后续评价/须知
  - 需在「精选研学路线」与「研学基地介绍」之间（或「研学基地介绍」之后）新增两个 `<section>`
- **现状**：页面只有路线、基地介绍、评价、须知等，缺"师资""课程体系"展示。
- **改动**：新增两个区块，沿用现有 `sec-title` / `sec-more` 标题样式与卡片样式，内联真实感 mock 数据：
  1. **师资团队**：3–4 张讲师卡片（头像/姓名/头衔/简介，如"非遗传承人""智能制造高级工程师"等）。
  2. **课程体系**：4–6 个课程模块卡片（课程名/时长/适合学段/亮点，如"袜机原理体验""纺织科普""VR 全景研学"等）。

### 4. 「展位申请」审核通过页增加"去缴费"按钮入口 〔待办〕
- **功能模块**：展会模块 → 买家端「展位申请」（`expo-booth-apply.html`）
- **关联后台 PRD**：袜博会管理 14b —— 展位申请审核（pending→approved→confirmed）、展位缴费（线下对公转账 + 财务手工确认收款）
- **精确位置**：
  - 状态回显容器：`#boothStatus`（约第 154 行）
  - 渲染函数：`function renderBoothStatus()`，其中 `apply.status==='approved'` 分支（约第 258–269 行，已显示"待缴费"徽标、展位号、应付金额、缴费方式说明，及"（演示）模拟财务确认收款"按钮 `confirmPay`）
- **现状**：已具备"待缴费"状态与"模拟财务确认收款"按钮，但缺一个明确的**"去缴费"引导 CTA**（引导查看转账信息/对公账户），缴费动作闭环不直观。
- **改动**：在 `approved` 分支「应付金额」之后、现有"模拟财务确认收款"按钮之前，新增「去缴费」按钮（点击打开缴费确认面板 / `toast` 展示对公转账信息：户名、账号、开户行、金额、截止日）。保留原"模拟财务确认收款"作为最终确认步骤，二者区分清晰。

### 5. 内联 `backend-config.js` 数据并删除该文件（架构清理） 〔已完成 · 2026-09-10 主线程直接完成〕
- **功能模块**：架构清理（非功能改动）—— 取消"前端读取共享后台配置"的模拟架构，各页改为内联 mock 数据
- **精确位置**：8 个文件各自的 `<script src="backend-config.js"></script>` 引用行 + 消费该配置的 JS 位置；`backend-config.js` 文件本身
- **涉及文件与需内联的数据子集**（只复制本页真正用到的部分，勿整文件照搬；复制后删除该页的 `<script src="backend-config.js">` 引用）：
  | 文件 | 引用的全局变量 / 函数 | 主要消费位置 |
  |---|---|---|
  | `expo.html` | `EXPO_CFG`（edition / scale / exhibitors / archives / info） | 首页展会头图、规模数据、精选展商、往届回顾、展会介绍 |
  | `expo-info.html` | `EXPO_CFG`（几乎全量：edition / scale / boothRows / exhibitors / archives / info） | 展会介绍页各区块 |
  | `expo-schedule.html` | `EXPO_SESSIONS` + `expoSessionsByDay(day)` | 按天渲染议程列表 |
  | `expo-session-detail.html` | `EXPO_SESSIONS` + `expoSessionsByDay`（按 id 取单条） | 议程详情 |
  | `study-tour.html` | `STUDY_CFG`（bases / routes / reviews / notices / reschedule） | 路线、基地、评价、须知、改期通知 |
  | `study-route.html` | `STUDY_CFG`（routes / sessions） | 路线余席、场次列表 |
  | `study-personal.html` | `STUDY_CFG`（liability 个人/团体责任书模板） | 责任书条款 |
  | `study-apply.html` | `STUDY_CFG`（routes：`cfg.left` / `cfg.seatStatus`） | 提交时名额/候补校验 |
- **注意**：
  - `study-tour.html` 同时含**第 6 项**改动、`study-apply.html` 同时含**第 2 项**改动，请在同一文件内一并完成。
  - 本项**不**涉及：`study-my.html`、`expo-booth-apply.html`、`expo-visitor-apply.html`、`expo-materials.html`（它们不依赖 `backend-config.js`，勿动；`study-my` 只做第 1 项）。
  - 8 个文件全部内联完毕、删除各自引用后，再删除 `backend-config.js`。页面功能须与改动前完全一致。

### 6. 移动端「往期风采」对齐后台自动生成内容（研学活动内容 + 用户留言评价 + 照片） 〔待办〕
- **功能模块**：研学模块 → 买家端「研学首页 / 往期研学风采」（`study-tour.html`）、「研学路线详情 / 历史风采」（`study-route.html`）
- **关联后台 PRD**：研学管理 14 —— 研学宣传配置（2026-09-10 已精简为仅「往期风采」，且改为研学结束后系统自动生成，每条含 研学活动内容 / 用户留言评价 / 照片）
- **精确位置**：
  - `study-tour.html` 「往期研学风采」区块（约第 302–330 行）：当前为 4 张写死照片卡（图 + 标题），无内容描述、无评价。
  - `study-route.html` 「历史风采」Tab（`switchTab('history')` 分支，约第 355–375 行）：当前为 `r.history` 照片卡（图 + 标题 + 日期 + 人数）+ 场次/人数/评分统计，无活动内容描述，评价在另一 Tab。
- **现状**：两处「往期风采 / 历史风采」仅展示照片与标题，与后台自动生成的三要素（活动内容 / 用户评价 / 照片）不一致。
- **改动**（两处各自内联 mock，沿用现有卡片样式，勿搭前后端同步架构）：
  1. 新增数据结构 `var GLORIES=[{id,title,route,date,people,status,content,photos:[],reviews:[{name,role,rating,date,text}]}]`，内联 2–3 条，与后台示例对齐（浙江大学研学团 / 杭州学军中学研学团 / 织袜工艺体验营），含 `status:'shown'|'hidden'` 覆盖。
  2. `study-tour.html` 区块：由 `GLORIES` 渲染「研学回顾卡」，卡面含 研学团名 / 路线 / 时间 / 人数 + 一行活动内容描述 + 照片缩略 + 评价数；点击卡进入详情（本页弹层或复用 study-route 历史详情），详情含完整活动内容、全部照片、全部用户评价（头像 / 姓名 / 角色 / 星级 / 日期 / 内容）。
  3. `study-route.html` 历史风采 Tab：每条增加活动内容描述摘要与「N 条评价」入口，点卡片展示完整活动内容 + 照片 + 评价（扩展 `r.history` 字段 `content` / `reviews`）。
  4. 仅渲染 `status==='shown'` 条目，与后台「隐藏 / 展示」一致；隐藏条目不出现。
- **验收**：照片卡可点击、详情包含三要素（活动内容 / 评价 / 照片）、隐藏 / 展示状态与后台一致、无死按钮。

### 7. 买家端移除"报价 / 填写报价单"入口（采购询价改由卖家端承担） 〔已完成 · 2026-09-20 本窗口重建 4 页：inquiry.html 角色校正（我的发布/公开需求 两 Tab，移除"收到报价"）+ inquiry-demand.html 只读详情（移除去报价/样品/拼单）+ inquiry-mypublish.html（报价卡字段与商户端对齐、点击商家看介绍+评价、选标→查看订单、定制样品规则提示）+ inquiry-publish.html（资质认证前置卡点 + 样品定制需求开关）；招标 #10 资质卡点、#11 样品寄送（发布端开关驱动、成交默认寄样）已落位〕
- **功能模块**：商城模块 → 买家端采购询价（仅保留：发布需求 / 查看需求 / 查看报价 / 选标成交 / 查看订单）
- **关联后台 PRD**：`开发PRD/modules/10a-采购询价管理.md`（决策 4，2026-09-17 已确认——报价入口移到卖家端）
- **精确位置**：
  - `mai-APP/inquiry.html`：「去报价」按钮（列表卡片/详情的报价引导）
  - `mai-APP/inquiry-demand.html`：「填写我的报价单」整段提交表单
- **现状**：买家端 App 当前含「去报价 / 填写我的报价单」入口，与"报价移到卖家端"的决策冲突；且买家端身份不应具备报价能力（报价是卖家行为）。
- **改动**：
  1. `inquiry.html` 的「去报价」按钮改为**仅展示**需求信息（发布人 / 品类 / 数量 / 期望价 / 报价截止倒计时 / 已报价家数），不出现任何"去报价"动作。
  2. `inquiry-demand.html` 整段「填写我的报价单」表单改为**只读展示**（报价统计、竞价结果、各卖家报价摘要但不暴露他人具体报价细节），不出现提交报价行为。
  3. 买家端保留的入口：发布需求、查看需求、查看报价、选标成交、查看订单，逻辑与现有页面一致。
- **验收**：买家端已无"报价 / 填写报价单"入口；需求查看、选标成交闭环完整；无死按钮。
- **备注**：卖家端对应的"新增需求池与报价流程"改动记录在 `shanghu-APP/移动端原型修改待办.md` 第 1 条，本文件不重复。

### 8. 询价成交后生成「采购询价订单」，买家端订单页需支持该类型 〔待办 · 2026-09-20 进度：inquiry-mypublish.html 选标已生成 QJ 订单号并跳转 orders.html，订单类型承接待本项落地〕
- **功能模块**：商城模块 → 买家端订单（订单类型：采购询价订单）
- **关联后台 PRD**：`开发PRD/modules/10a-采购询价管理.md`——买家选标成交后系统自动生成询价订单（不关联上架 SKU，商品明细=需求快照，价格=成交报价），主数据在外采订单库。
- **精确位置**：
  - 买家端 `mai-APP/orders.html`（状态 Tab：全部/待付款/待发货/待收货/待评价/售后）、`order-detail.html`
  - 买家端 `mai-APP/inquiry-mypublish.html`（选标确认成功后的跳转引导）
- **现状**：买家端订单页为纯商品订单结构（店铺+商品图+SKU+单价×数量），不认识询价订单类型。2026-09-20 已接好：inquiry-mypublish.html 选标成交即生成 QJ 订单号、需求置「已成交」并跳转 orders.html；但 orders.html 询价订单类型尚未落地，故订单暂无处呈现、无法付款。
- **改动**：
  1. 买家端「我的订单」列表支持询价订单类型：卡片以**需求标题**代替商品名+SKU，加「询价订单」角标，显示 成交单价 × 数量、关联需求单号（可点回需求详情），状态标签复用现有状态体系（待付款/待发货/待收货/已完成）。
  2. 买家端订单详情：增加**需求溯源块**（原始需求 → 成交报价：单价/数量/交期承诺/样品支持/是否含运费）；操作按状态（待付款→去付款；待发货→查看交期/催发货；已完成→评价）。
  3. 买家端 `inquiry-mypublish.html` 选标确认成功后的跳转文案增加"订单已生成，去付款"引导（现为回到 inquiry.html）。
- **验收**：询价订单在买家端列表/详情可见、状态操作完整（尤其去付款）、与普通订单样式可区分；需求单 ⇄ 订单可互跳。
- **备注**：卖家端对应的"询价订单支持"改动记录在 `shanghu-APP/移动端原型修改待办.md` 第 2 条，本文件不重复。

---

## 新增记录模板（后续每次发现买家端需修改时，复制追加）

### 9. 买家端移除底部「物业」Tab（物业服务实际归属商户端） 〔已完成 · 2026-09-20 本窗口直接执行：全局移除 property.html Tab 引用 + 删除 property.html，导航降为 4 Tab：首页/视频看货/进货单/我的〕
- **功能模块**：物业服务 → 买家端底部导航（售前演示误放，实际归属卖家端）
- **关联后台 PRD**：`开发PRD/商户端APP-PRD.md` §4.5 / §6.1（Q1 确认：物业服务归商户端，图标平铺）
- **精确位置**：`mai-APP/` 下所有含底部 Tab 栏页面的这一行（约 27 个文件，行内文本一致）：
  `<a href="property.html" class="flex flex-col items-center gap-0.5 text-gray-400"><i class="fas fa-building text-[18px]"></i><span class="text-[9px]">物业</span></a>`
  另 `mai-APP/property.html` 自身第 130 行为高亮态版本（该文件随迁后买家端不再保留）。
- **现状**：物业服务枢纽页放在买家端底部 Tab 第 4 位，商户端才是其归属端（已挪入 `shanghu-APP/property.html` 并接通内链，见卖家端待办第 3 条）。
- **改动**：
  1. 批量删除上述 `<a href="property.html">物业</a>` Tab 项（底部 Tab 由 5 个变 4 个：首页/视频看货/进货单/我的，注意保持 `justify-around` 布局即可，无需改样式）。
  2. 删除买家端 `mai-APP/property.html`（已迁移），避免双端重复维护。
- **验收**：买家端所有页面底部无"物业"Tab；全站搜索 `href="property.html"` 仅剩（应为 0）处残留。

### N. 〔标题〕 〔待办〕
- **功能模块**：（研学模块 / 展会模块 → 具体子功能）
- **关联后台 PRD**：（对应后台页面 / PRD 编号 / 状态机）
- **精确位置**：（文件 + DOM id / JS 函数 / 行号）
- **现状**：（当前缺什么）
- **改动**：（要加什么、点哪、跳转哪）
- **状态**：待办
